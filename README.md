# NS_CIA_1
This is a spy app. The aim is to send encrypted message in the form of morse code. The app does this by using ports and switches to communicate the message to server. And then the server decrypts the message.
